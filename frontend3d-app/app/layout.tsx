export const metadata = {
  title: "AI Contract Analyzer",
  description: "3D AI-powered contract analysis",
};

import "./globals.css";

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}